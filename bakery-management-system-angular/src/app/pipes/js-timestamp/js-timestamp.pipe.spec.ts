import { JsTimestampPipe } from './js-timestamp.pipe';

describe('JsTimestampPipe', () => {
  it('create an instance', () => {
    const pipe = new JsTimestampPipe();
    expect(pipe).toBeTruthy();
  });
});
